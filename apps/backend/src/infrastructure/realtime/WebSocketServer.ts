export class WebSocketServer {
  public broadcast(eventType: string, payload: object): void {
    void eventType;
    void payload;
  }

  public broadcastToRoom(roomCode: string, eventType: string, payload: object): void {
    void roomCode;
    void eventType;
    void payload;
  }
}
